buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // Use the version compatible with your setup
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
